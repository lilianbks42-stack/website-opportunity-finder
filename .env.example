# Copy this file to ".env" and fill in real values later.
# Nothing here is required yet for Phase 1 — this is just a placeholder
# so the project structure is ready for Phase 2 (connecting a search source).

# Example for a future search API key (not used yet):
# SEARCH_API_KEY=

# Example for future Google Sheets integration (not used yet):
# GOOGLE_SHEETS_CREDENTIALS_PATH=
